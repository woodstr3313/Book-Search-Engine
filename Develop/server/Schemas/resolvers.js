const resolvers = {
    Mutation:{
        login(email, password) {},
        addUser(email, password){},
        saveBook(book){},
        removeBook(bookId){}
    },
  };


module.exports = resolvers;
